<div id="comments">

<table>
			   
				<% @book.book_comments.each do |book_comment| %>
				<tr>
				<td>
				<%= attachment_image_tag book_comment.user, :profile_image, :fill, 100, 100, fallback: "no-image-mini.jpg" %><br>
				<%= book_comment.user.name %>
			    </td>
			
			
			    <td>
			    <%= book_comment.comment %>
			    </td>
			    <% if book_comment.user.id == current_user.id %>
			    <td><%= link_to "Destroy",book_book_comments_path(@book), method: :delete, remote: true, class: "btn-sm btn-danger" %>
			    </td>
			    
			    <%end%>
			    </tr>
			    <%end%>
			 
	    	</table>
</div>